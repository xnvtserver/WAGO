You are an expert Vue.js 3 developer. 
Create authentication views (Login and Register) using Composition API and TailwindCSS.

Requirements:
- LoginView.vue and RegisterView.vue inside /views/auth.
- Each should have:
  - Input fields with v-model (email, password, confirmPassword in register).
  - Submit button.
  - Basic form validation (required fields).
- On submit → call a mock API function from /services/api.js.
- Use Pinia auth store to save user session (isAuthenticated, role).
- Provide navigation links (Login <-> Register).
Output full code for LoginView.vue, RegisterView.vue, and updates to /stores/auth.js + /services/api.js with mock auth functions.
