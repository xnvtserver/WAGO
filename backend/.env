# -------------------------------
# Server Configuration
# -------------------------------

# -------------------------------
# Frontend URLs (CORS allowed)
# -------------------------------
FRONTEND_URL=http://localhost:5177
FRONTEND_URL_DEV=http://localhost:5177

# -------------------------------
# Database Configuration
# -------------------------------
# Database config
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=vishal@123
DB_NAME=wago
# To enter db on terminal : psql -h localhost -p 5432 -U postgres -d wago

# -------------------------------
# Security / Tokens
# -------------------------------
JWT_SECRET=supersecretkey   # change to a strong random value

# -------------------------------
# File Uploads
# -------------------------------
UPLOAD_DIR=public/uploads
MAX_FILE_SIZE=5242880   # 5MB in bytes


# JWT config
JWT_SECRET=my_jwt_secret_key
JWT_EXPIRES_IN=1d

# File upload config
FILE_UPLOAD_PATH=./uploads
MAX_FILE_UPLOAD=5

# Properly encoded DATABASE_URL (note: password with @ must be URL encoded as %40)
DATABASE_URL=postgres://postgres:vishal%40123@localhost:5432/LMS